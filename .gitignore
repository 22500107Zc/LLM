v-env
.env
!.env.example

node_modules
__pycache__
v-env
.DS_Store
aws_cf_deploy_anything_llm.json
yarn.lock
*.bak
.idea

# Local deployment environment files (never commit secrets)
server/.env
server/.env.production
docker/.env

# Test run output (evidence only; never part of the runtime image)
test-results/

# Dedicated customer deployments: generated .env files (secrets), reverse-proxy
# configs and backups. Never commit any of this.
deployments/
backups/

# Generated test fixtures and Stripe CLI artefacts, in case a run is pointed at
# the repository instead of a temporary directory.
scripts/fixtures/
.stripe/
stripe-cli-*
*.stripe-events.json
